[package]
name = "retrieval"
edition.workspace = true
license.workspace = true
version.workspace = true

[dependencies]
embeddings = { path = "../embeddings" }
lorelm-core = { path = "../core" }
storage = { path = "../storage" }
thiserror.workspace = true
